rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // แต่ละคนอ่าน/เขียนได้เฉพาะข้อมูลของตัวเอง
    match /users/{uid}/mindcars/{doc} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
